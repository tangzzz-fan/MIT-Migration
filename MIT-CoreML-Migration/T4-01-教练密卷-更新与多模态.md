# T4-01 教练密卷：更新与多模态

- C1：看模型是否以 updatable 训练/转换、是否有更新任务 API、是否有本地可写存储与用户同意。
- C2：过拟合用户、漂移、存储胀。
- C3：系统 NLP 够用→内建；垂直领域/自定义标签→CoreML。
- C4：最小/最大边、非正方形、极端长宽比、内存峰值。
- C5：版本钉扎、校验和、双缓冲、失败回退旧模型。
- C6：对齐时间戳/采样率、缺失模态降级、体积。

代码：清单完整；NLTagger 有输出；版本切换可打印；契约字段≥4。
